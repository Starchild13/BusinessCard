# DEMO ERROR: This line intentionally causes a Missing Android SDK error.
# EXPLANATION: The sdk.dir is pointed to a non-existent directory. 
# Gradle will fail because it cannot find the Android SDK required to build the project.
sdk.dir=/path/to/nonexistent/android/sdk

# DEMO SOLUTION:
# Replace the incorrect configuration above with:
# sdk.dir=/Users/<your-username>/Library/Android/sdk (or your actual SDK path)
# EXPLANATION: Pointing to a valid Android SDK directory allows the build system to find tools and platforms needed for compilation.
# Do not apply this solution yet. It is intentionally commented out for the demo.
