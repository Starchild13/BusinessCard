plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.plugin.compose'
}

android {
    // ... rest of android block ...
}

// DEMO ERROR: This block intentionally causes a Prolonged build time.
// EXPLANATION: This custom task 'slowTask' uses Thread.sleep to simulate a 30-second delay. 
// Making it a dependency of 'preBuild' ensures it runs every time the project is built, significantly increasing build times.
tasks.register("slowTask") {
    doLast {
        println("Simulating a very slow build step (30 seconds)...")
        Thread.sleep(30000)
    }
}
tasks.named("preBuild") {
    dependsOn("slowTask")
}

// DEMO SOLUTION:
// Replace the incorrect configuration above with:
// // (Remove the slowTask registration and the dependency on it)
// EXPLANATION: Removing artificial delays or optimizing slow custom tasks restores build performance.
// Do not apply this solution yet. It is intentionally commented out for the demo.

dependencies {

    // Compose UI Testing
    androidTestImplementation "androidx.compose.ui:ui-test-junit4:1.11.4"
    debugImplementation "androidx.compose.ui:ui-test-manifest:1.11.4"

    // Intent testing
    androidTestImplementation "androidx.test.espresso:espresso-intents:3.7.0"

    // Screenshot testing (optional)
    testImplementation "app.cash.paparazzi:paparazzi:1.3.5"

    implementation 'androidx.core:core-ktx:1.19.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.11.0'
    implementation 'androidx.activity:activity-compose:1.13.0'
    implementation "androidx.compose.ui:ui:1.11.4"
    implementation "androidx.compose.ui:ui-tooling-preview:1.11.4"
    implementation 'androidx.compose.material:material:1.11.4'
    implementation 'androidx.compose.material3:material3:1.4.0'
    implementation 'androidx.test.espresso:espresso-web:3.7.0'
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.3.0'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.7.0'
    androidTestImplementation "androidx.compose.ui:ui-test-junit4:1.11.4"
    debugImplementation "androidx.compose.ui:ui-tooling:1.11.4"
    debugImplementation "androidx.compose.ui:ui-test-manifest:1.11.4"
    // If you’re using the Compose BOM:
    implementation platform("androidx.compose:compose-bom:2026.06.01")

// …then pull in material-icons-extended:
    implementation "androidx.compose.material:material-icons-extended"
}